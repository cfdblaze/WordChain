<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.blaze.wordchain" >

    <application android:name=".GlobalVars"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Builder"
            android:label="Builder" >
        </activity>
        <activity
            android:name=".WordChain"
            android:label="@string/title_activity_word_chain" >
        </activity>
        <activity
            android:name=".Solver"
            android:label="@string/title_activity_solver"
            android:parentActivityName=".WordChain" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="WordChain" />
        </activity>
        <activity
            android:name=".Loader"
            android:label="@string/title_activity_loader" >
        </activity>
    </application>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
</manifest>
