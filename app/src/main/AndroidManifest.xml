<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.blaze.wordchain" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".BuildPage"
            android:label="@string/title_activity_build_page" >
        </activity>
        <activity
            android:name=".SolvePage"
            android:label="@string/title_activity_solve_page" >
        </activity>
        <activity
            android:name=".LoadActivity"
            android:label="@string/title_activity_load" >
        </activity>
        <activity
            android:name=".LoadPage"
            android:label="@string/title_activity_load_page" >
        </activity>
    </application>

</manifest>
